import "../../main.del";
import ".buffMain.del";

rule: "[Buff] :: Berserk : Set Status"
Event.OngoingPlayer
if (buffEnabled[4])
{
    infFuel = true;
    infAmmo = true;
    delay = 0.5;

    bhopSpeedFloor *= 2;
    bhopSpeedRate = S_BHOP_SPEED_RATE * 2.00;
    bhopSpeedPercent *= 2;
    SetMoveSpeed(player, bhopSpeedPercent);
}