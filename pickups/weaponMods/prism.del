import "../../main.del";
import ".modMain.del";


disabled rule: "---------------- Prism ----------------"{}

rule: "[Mod] :: Prism : Fire Action"
Event.OngoingPlayer
if (modEnabled[Mod.Prism])
if (IsFiringPrimary())
{
    
}